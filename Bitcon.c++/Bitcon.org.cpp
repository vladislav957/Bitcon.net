#include "pch.h"
#include "CppUnitTest.h"
#include<iostream>
#include<string>

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace Bitconorg
{
	TEST_CLASS(Bitconorg)
	{
	public:
		
		TEST_METHOD(TestMethod1);
		
		class CryptoCurrency {
		int:

			 _wfinddata64_t::name[260];
			double price;
		public:
			CryptoCurrency(std::string n, double p) : name(n), price(p) {}


			CryptoCurrency() = default;
		};

		int main() {
			CryptoCurrency
				bitcon("Bitcon", 65000.50);
			bitcon.printlnfo();
			return 0;
		};